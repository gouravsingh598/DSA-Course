Problem Link:-https://practice.geeksforgeeks.org/problems/professor-and-parties2000/1
Solution :- 
    string PartyType(long long int arr[], int n)
    {
        unordered_set<int> s;
        for(int i=0;i<n;i++)
        {
            s.insert(arr[i]);
        }
        if(s.size()==n)
        return "GIRLS";
        else
        return "BOYS";
    }
